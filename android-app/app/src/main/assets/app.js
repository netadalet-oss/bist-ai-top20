'use strict';
const SOURCE_LOCK='5fbe5a2df9ec7a28f8e8b87d4648a8f1f0826dd2';
const state={page:'overview',api:localStorage.getItem('aurumApi')||'',data:null,error:''};
const pages={overview:'Genel Bakış',selection:'S Listesi',models:'K1–K5',performance:'Performans',quality:'Veri Kalitesi',settings:'Ayarlar'};
const content=document.querySelector('#content'),title=document.querySelector('#title');
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{state.page=b.dataset.page;document.querySelectorAll('nav button').forEach(x=>x.classList.toggle('active',x===b));render()});
function metric(label,value,sub=''){return `<div class="metric"><span class="muted">${label}</span><strong>${value}</strong><small class="muted">${sub}</small></div>`}
function rows(items){return items.map(([a,b,c=''])=>`<div class="row"><span>${a}</span><span class="${c}">${b}</span></div>`).join('')}
function overview(){return `<section class="hero"><h2>Reel Top 20 gölge terminali</h2><p class="muted">Aynı gün ve sonraki işlem günü adaylarını K1–K5, veri kalite kapısı, değişmez snapshot, outcome ve net getiri sözleşmeleriyle izler. Aktif yatırım sinyali üretmez.</p></section><section class="grid">${metric('Çalışma modu','SHADOW','Üretime kapalı')}${metric('Kaynak kilidi',SOURCE_LOCK.slice(0,8),'Değişmez referans')}${metric('Kanonik şema','468','A:QZ')}${metric('Canlı evren','556','Son denetim')}</section><section class="card"><h3>Canlı veri sonucu</h3>${rows([['Temel teknik kapsam','%99,82',''],['Hacim değişimi kapsamı','%99,46',''],['SNKRN','Tüm modellerden dışla','danger'],['UMPAS / YGYO','K3 hacim teyidinden dışla','warn']])}</section>`}
function selection(){const list=state.data?.selection||[];return `<section class="card"><h3>SAME_DAY / NEXT_DAY</h3><p class="muted">API bağlandığında değişmez snapshot sırası, giriş fiyatı, kaynak modeller ve iki horizon skoru burada gösterilir.</p>${list.length?list.map(x=>`<div class="row"><b>${x.rank}. ${x.symbol}</b><span>${x.score??'-'}</span></div>`).join(''):'<p class="muted">Henüz runtime snapshotı yok.</p>'}</section>`}
function models(){return `<section class="card"><h3>Uzman modeller</h3>${rows([['K1','Momentum · volatilite · Bollinger'],['K2','EMA · MACD · RSI · Momentum'],['K3','Hisse bazlı dipten toparlanma'],['K4','Çoklu dönem liderlik'],['K5','En az iki model konsensüsü']])}</section><section class="card"><h3>Karantinadaki hipotezler</h3>${rows([['K1','Yüksek volatilite ödülü','warn'],['K2','RSI 55 simetrisi','warn'],['K3','Derin dip ödülü','warn']])}</section>`}
function performance(){return `<section class="grid">${metric('Precision@20','—','Snapshot bekleniyor')}${metric('Recall@20','—','Outcome bekleniyor')}${metric('Net getiri','—','Maliyet sonrası')}${metric('MFE / MAE','—','Ham barlardan')}</section><section class="card"><h3>Ölçüm sözleşmesi</h3>${rows([['Giriş fiyatı','Tahmin anında sabit'],['İlk Top 20 girişi','Zaman ve getiri'],['Legacy / New','Aynı outcome'],['Bootstrap','Fold seviyesinde']])}</section>`}
function quality(){return `<section class="card"><h3>Fail-closed kapılar</h3>${rows([['Apps Script bütünlüğü','Veri okunmadan önce'],['Model özellik kapsamı','Varsayılan eşik %80'],['Başlık normalizasyonu','U+2060 temizliği'],['Takvim','FULL / HALF / CLOSED']])}</section><section class="card"><h3>Bilinen dışlamalar</h3>${rows([['SNKRN','Temel ve tarihsel veri eksik','danger'],['UMPAS','Hacim değişimi tarihçesi eksik','warn'],['YGYO','Hacim değişimi tarihçesi eksik','warn']])}</section>`}
function settings(){return `<section class="card"><h3>Apps Script API</h3><label class="muted">HTTPS Web App adresi</label><input id="api" value="${state.api}" placeholder="https://script.google.com/macros/s/.../exec"><button class="primary" id="save">Kaydet ve dene</button>${state.error?`<p class="danger">${state.error}</p>`:''}<pre>applicationId: com.aurum.shadowt20\nsourceLock: ${SOURCE_LOCK}\nmode: SHADOW_ONLY</pre></section>`}
function render(){title.textContent=pages[state.page];content.innerHTML=({overview,selection,models,performance,quality,settings}[state.page])();if(state.page==='settings')document.querySelector('#save').onclick=saveApi}
async function saveApi(){state.api=document.querySelector('#api').value.trim();if(state.api&&!state.api.startsWith('https://')){state.error='Yalnız HTTPS adresi kabul edilir.';return render()}localStorage.setItem('aurumApi',state.api);state.error='';if(state.api){try{const r=await fetch(`${state.api}${state.api.includes('?')?'&':'?'}action=status`);state.data=await r.json()}catch(e){state.error=e.message}}render()}
render();
